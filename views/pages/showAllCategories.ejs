<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/styles.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <%- include('../partials/header') %>
    
    <div class="container mx-auto px-4 py-8">
        <div class="mb-6">
            <a href="/categories/new" class="bg-white border-2 border-gray-300 px-4 py-2 text-blue-600 font-semibold">Add New Category</a>
        </div>
        
        <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center"><%= heading %></h1>

        <ul class="space-y-4">
            <% categories.map((category, index) => { %>
                <li>
                    <div class="bg-white border-2 border-gray-300 p-6 flex justify-between">
                        <a href="/categories/<%= category.category_id %>/items" class="text-blue-600 font-semibold text-lg">
                            <%= category.category_name %>
                        </a>
                        <form action="/categories/<%= category.category_id %>/delete" method="POST">
                            <button type="submit" onclick="return confirm('Do you really wanna delete this category?')">Delete</button>
                        </form>
                    </div>
                </li>
            <%}) %>
        </ul>
    </div>
    
</body>
</html>